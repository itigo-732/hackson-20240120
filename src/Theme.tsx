import { StyleSheet, Platform, StatusBar } from 'react-native';

export default const ThemeColor = StyleSheet.create({
    main: {
        backgroundColor: '#91CCF2',
        color: 'white',
    },

    base: {
        backgroundColor: 'white',
        color: 'black',
    },

    accent: {
        backgroundColor: 'red',
        color: 'white',
    },
});